#include <stdio.h>
#include <string.h>

int main(void)
{
    char ca1[] = {'C', '+', '+'};
    printf("sizeof of ca1 is %d\n", (int)sizeof(ca1));
    printf("strlen of ca1 is %d\n", (int)strlen(ca1));
    return 0;
}
